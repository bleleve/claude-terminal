{
  "name": "claude-terminal-hub-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/reliability.test.cjs",
    "types": "wrangler types --env-interface HubEnv --include-runtime=false"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260915.1",
    "miniflare": "^5.20260911.1-alpha",
    "wrangler": "^4.131.2"
  }
}
